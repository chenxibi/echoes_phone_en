# 2026-04-15

## English Localization Work (echoes_phone_en)

### Completed
- **stickers.js**: All 70+ sticker descriptions translated from Chinese to English
- **appHelpers.jsx**: All UI-facing Chinese translated (StatusPanel labels, CreationAssistantModal, StickerEditorModal, TransferBubble, GhostButton, VoiceMessageBubble, formatDate locale, error messages)
- **App.jsx**: Fixed garbled regex (`/[:：]/`) at line 173 that was causing esbuild to fail. Fixed "Plain Sketch" prompt text, removed Chinese character count references
- **Music.jsx**: Translated the inline music prompt guidelines from Chinese to English
- **helpers.js**: formatDate locale already English, no other UI text changes needed

### Completed (continued)
- **prompts.js**: Sub-agent rewrote the entire file. Zero garbled characters remaining. All English translations applied: forum nicknames → English usernames (BubbleTeaBear, MidnightSoliloquy, ProudDad2025, literally_dying_rn, etc.), 回复→@, 卧槽/哈哈/确实/666→omg/lmao/fr fr/based, all language instructions → English, CHARACTER_CREATION_PROMPT fully rewritten
- **prompts_en.js**: Copied clean prompts.js content with renamed exports (DEFAULT_PROMPTS→DEFAULT_PROMPTS_EN, STYLE_PROMPTS→STYLE_PROMPTS_EN) since App.jsx imports from this file
- **App.jsx additional fixes**: ¥ transfer amount display (line 478/487), "Plain Sketch" dict label (line 1824), French café name "La Crêmerie" (line 2504), 📍 emoji in SmartWatch status (line 4987)

### Final State
- All UI-facing text is English. Remaining Chinese in codebase is ONLY in code comments (`//` and `{/* */}` blocks) — per user request, left untouched
- No more esbuild errors. App should build and run correctly.

### Notes
- User rule: code comments in Chinese don't need to be changed, only UI-facing text and prompts
- Large text blocks should be translated as a whole, not character-by-character replaced
- PowerShell `Get-Content`/string replacement can corrupt Chinese characters — use `[System.IO.File]::ReadAllBytes` + UTF-8 encoding for safe operations

### Bug Fixed
- esbuild error "Unterminated regular expression" at App.jsx:173 — caused by corrupted Chinese fullwidth colon (：) in regex `/[:：]/` becoming garbled bytes

### Notes
- User rule: code comments in Chinese don't need to be changed, only UI-facing text and prompts
- Large text blocks should be translated as a whole, not character-by-character replaced
- PowerShell `Get-Content`/string replacement can corrupt Chinese characters — use `[System.IO.File]::ReadAllBytes` + UTF-8 encoding for safe operations
